apiVersion: miniocontroller.minio.io/v1beta1
kind: MinioInstance
metadata:
  name: minio1
spec:
  replicas: 4
  credsSecret:
    name: minio-creds-secret # Create the secret using the file ./minio-creds-secret.yaml
